#include <stdio.h>

/**
 * main - prints zero to nine
 *
 * Return: Always(success)
 */
int main(void)
{
	int number;

	for (number = '0'; number <= '9'; number++)
		putchar(number);
	putchar('\n');
	retur (0);
}
